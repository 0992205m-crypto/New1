<manifest xmlns:android="http://android.com"
    package="com.example.cast_browser_app">
    
    <!-- 1. صلاحية الوصول للإنترنت لفتح المواقع داخل المتصفح -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- 2. صلاحية فحص حالة الـ Wi-Fi للبحث عن الشاشات -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- 3. صلاحية أساسية لمكتبة UPnP لاستكشاف الأجهزة المحلية (Multicast) -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    
    <!-- 4. صلاحية الوصول للموقع الجغرافي (مطلوبة في بعض إصدارات أندرويد لربط الـ Wi-Fi) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

   <application
        android:label="كاست براوزر الذكي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|locale|layoutDirection|fontScale|screenSize|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
